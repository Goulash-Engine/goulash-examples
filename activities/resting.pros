activity resting {
	trigger_urges ["rest"]
	blocker_conditions ["rested"]
	duration ["30"]
	logic on_abort {
    actor::condition(energized).set()
	}
	logic act {
		actor::urge(rest).minus(30);
		actor::urge(think).plus(5);
	}
}

